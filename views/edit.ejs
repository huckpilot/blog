<!DOCTYPE html>
<body>
  <!-- Update post -->
  <h3>Update <%= thisPost.title %>:</h3>
  <form method="POST" action="/post/<%= thisPost.id %>?_method=PUT">
    <input type="text" name="title" placeholder="<%= thisPost.id %>" required>
    <input type="text" name="title" placeholder="<%= thisPost.paragraph %>" required>
    <button>You have updated your post</button>
  </form>
</body>
</html>

